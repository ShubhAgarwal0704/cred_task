name: cred_assignment
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  animated_toggle_switch: ^0.8.3
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  group_grid_view: ^1.0.1
  http: ^1.2.2
  neopop: ^1.0.2
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  switcher_xlive: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
